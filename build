#!/bin/bash

# Build the client
cd frontend && ./build

# Copy over the javascript
rm ../backend/static/all.js
cp $(stack path --local-install-root)/bin/frontend.jsexe/all.js ../backend/static/all.js
cd ..

# Build the server
cd backend && ./build && cd ..
